<Window x:Class="WorkTimeTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Work Time Tracker"
        Height="500" Width="760"
        FontFamily="Segoe UI"
        FontSize="13"
        Background="#F8F9FB">

    <DockPanel Margin="20">

        <!-- ====== Shared styles/resources ====== -->
        <DockPanel.Resources>
            <!-- Remove blue selection highlight -->
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>

            <Style TargetType="DataGridCell">
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
            </Style>

            <!-- Rounded button template -->
            <Style TargetType="Button">
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="6,2"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="root"
                                    CornerRadius="3"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Margin="2"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="root" Property="Opacity" Value="0.85"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="root" Property="Opacity" Value="0.6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="Content" Value="Start">
                        <Setter Property="Background" Value="#44C767"/>
                        <Setter Property="BorderBrush" Value="#3AAA59"/>
                    </Trigger>
                    <Trigger Property="Content" Value="Stop">
                        <Setter Property="Background" Value="#E74C3C"/>
                        <Setter Property="BorderBrush" Value="#C84133"/>
                    </Trigger>
                    <Trigger Property="Content" Value="➕ Add Task">
                        <Setter Property="Background" Value="#3C91E6"/>
                        <Setter Property="BorderBrush" Value="#2B6FB6"/>
                    </Trigger>
                    <Trigger Property="Content" Value="X">
                        <Setter Property="Background" Value="#999999"/>
                        <Setter Property="BorderBrush" Value="#7A7A7A"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Column header style -->
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#F3F3F3"/>
                <Setter Property="Foreground" Value="#444"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="BorderBrush" Value="#DDD"/>
                <Setter Property="Padding" Value="6,3"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
            </Style>
        </DockPanel.Resources>

        <!-- ====== Header area ====== -->
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical" Grid.Column="0">
                <TextBlock Text="{Binding CurrentTime}"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="#333"
                           Margin="0,0,0,4"/>
                <TextBlock Text="{Binding TotalTimeFormatted}"
                           FontSize="18"
                           Foreground="#666"/>
            </StackPanel>

            <Button Content="➕ Add Task"
                    Command="{Binding AddTaskCommand}"
                    FontWeight="Bold"
                    FontSize="14"
                    Width="140"
                    Padding="6"
                    Margin="10,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- ====== DataGrid Container ====== -->
        <Border CornerRadius="6"
                Background="White"
                BorderBrush="#DDD"
                BorderThickness="1"
                SnapsToDevicePixels="True"
                Padding="0">

            <DataGrid ItemsSource="{Binding Tasks}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeight="42"
                      Background="Transparent"
                      BorderThickness="0"
                      HorizontalGridLinesBrush="#EEE"
                      Margin="0"
                      AlternationCount="2"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      SelectedItem="{x:Null}"
                      EnableRowVirtualization="False"
                      EnableColumnVirtualization="False">

                <!-- === RowStyle binds to ViewModel brush === -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{Binding RowBrush}"/>
                        <Style.Triggers>
                            <!-- Alternate slight tint -->
                            <Trigger Property="AlternationIndex" Value="1">
                                <Setter Property="Opacity" Value="0.96"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <!-- === Columns === -->
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Task"
                                        Binding="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Width="*"/>
                    <DataGridTextColumn Header="Elapsed"
                                        Binding="{Binding ElapsedFormatted}"
                                        Width="120"
                                        IsReadOnly="True"/>

                    <DataGridTemplateColumn Header="Controls" Width="210" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Start"
                                            Command="{Binding StartCommand}"
                                            Width="60" Margin="3"/>
                                    <Button Content="Stop"
                                            Command="{Binding StopCommand}"
                                            Width="60" Margin="3"/>
                                    <Button Content="X"
                                            Command="{Binding DataContext.RemoveTaskCommand,
                                                              RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Width="30" Margin="3"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</Window>
